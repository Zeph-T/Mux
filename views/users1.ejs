<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Electronic Users</title>

    <style>
        input,
        select {
            padding: 12px 20px;
            margin: 8px 0;
            display: inline-block;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        input {
            width: 100%;
            background-color: grey;
            color: white;
            padding: 10px 5px;
            margin: 8px 0;
            border: none;
            border-radius: 4px;
            align-items: center;
            cursor: pointer;
            font-size: 25px;
        }

        input[type=submit] {
            width: 25vw;
            text-align: center;
            align-items: center;
        }

        input[type=submit]:hover {
            background-color: #ccc;
        }

        label {
            font-size: 30px;
        }

        .container {
            margin-bottom: 50px;
            background-color: royalblue;
            padding: 30px;
            width: 40vw;
            border-radius: 10px;
            border: 4px solid black;
            margin-left: 30vw;
        }
    </style>
</head>

<body>
    <h1 style="text-align: center;font-size: 100px;">Users</h1>

    <% for(var i=0;i<users.length;i++){ %>
        <div class="container">
            <form action="/electronicusers/updateUser" method="post">
                <%for(var key in users[i]){ %>
                    <% if(key==="Username" ) { %>
                        <label for="">
                            <%= key %>
                        </label>
                        <input type="text" readonly="readonly" name="Username" value="<%= users[i][key]%>">
                        <% }else { %>

                            <label for="">
                                <%= key %>
                            </label>
                            <input type="text" name="<%= key %>" value="<%= users[i][key] %>" id="" required>
                            <% } %>
                                <% } %>

                                    <input type="submit" value="Update">
            </form>
            <form action="electronicusers/deleteUser" method="post">
                <div style="display:none;">
                    <input type="text" name="Username" value="<%= users[i].Username %>" />
                </div>
                <input style="background-color: red;" type="submit" value="Delete" />
            </form>

        </div>
        <% } %>
</body>

</html>